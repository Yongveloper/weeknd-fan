---
name: INTO:DAWN
description: 어두운 일식의 빛 아래 읽는 한국어 팬 팜플렛
colors:
  night: '#050507'
  red: '#a61f27'
  blue: '#2f63d8'
  amber: '#e6a359'
  ivory: '#f0e8da'
  mist: '#d7d8dc'
  ink-muted: 'color-mix(in srgb, var(--ivory) 70%, transparent)'
  eclipse-black: '#000'
  error: '#ffbac0'
typography:
  display:
    fontFamily: "'Bebas Neue', Impact, sans-serif"
    fontWeight: 400
    lineHeight: 0.82
    letterSpacing: '0.02em'
  headline:
    fontFamily: "'Noto Sans KR Variable', system-ui, sans-serif"
    fontSize: 'calc(var(--type-scale) * clamp(2.1rem, 5.5vw, 4rem))'
    fontWeight: 900
    lineHeight: 1.08
    letterSpacing: '-0.02em'
  title:
    fontFamily: "'Noto Sans KR Variable', system-ui, sans-serif"
    fontSize: 'calc(var(--type-scale) * clamp(1.2rem, 2.4vw, 1.5rem))'
    fontWeight: 900
    lineHeight: 1.08
    letterSpacing: '-0.02em'
  body:
    fontFamily: "'Noto Sans KR Variable', system-ui, sans-serif"
    fontSize: 'calc(var(--type-scale) * 1rem)'
    lineHeight: 1.6
  label:
    fontFamily: "'Noto Sans KR Variable', system-ui, sans-serif"
    fontSize: 'calc(var(--type-scale) * 0.72rem)'
    fontWeight: 800
    letterSpacing: '0.16em'
rounded:
  square: '0'
  filter: '0.25rem'
  circle: '50%'
spacing:
  gutter: 'clamp(1rem, 4vw, 4rem)'
  section-space: 'clamp(5rem, 13vw, 11rem)'
  lunar-section-space: 'clamp(4.5rem, 9vw, 8rem)'
  editorial-column-gap: 'clamp(3rem, 7vw, 7rem)'
components:
  ticket-button:
    backgroundColor: '{colors.amber}'
    textColor: '{colors.night}'
    rounded: '{rounded.square}'
    padding: '0.55rem 0.8rem'
  ticket-button-secondary:
    backgroundColor: 'transparent'
    textColor: '{colors.ivory}'
    rounded: '{rounded.square}'
    padding: '0.55rem 0.8rem'
  ticket-select:
    backgroundColor: '{colors.night}'
    textColor: '{colors.ivory}'
    rounded: '{rounded.square}'
    padding: '0.55rem 0.8rem'
    width: '100%'
  setlist-search:
    textColor: '{colors.ivory}'
    rounded: '{rounded.filter}'
    width: '100%'
  header-navigation:
    textColor: '{colors.ink-muted}'
    padding: '0.45rem 0.65rem'
  pamphlet-link:
    textColor: '{colors.ivory}'
    padding: '0.65rem 0'
  disclosure:
    textColor: '{colors.ivory}'
    padding: '0.9rem 0.2rem'
  status-expected:
    textColor: '{colors.amber}'
    padding: '0.3rem 0.65rem'
  share-fallback:
    textColor: '{colors.ivory}'
    padding: '1rem'
  body-link:
    textColor: '{colors.ivory}'
---

# Design System: INTO:DAWN

## Overview

**Creative North Star: "Til dawn — 일식의 빛 아래 읽는 팜플렛"**

칠흑 같은 밤에서 아침으로 향하는 순간을 담는다. 가려진 해의 비대칭 빛의 띠와 은은하게 빛 받은 안개구름이 공간을 만들고, 한국어 공연 자료는 또렷한 활자와 절제된 구분선으로 읽힌다. 일식은 달이 아니라 해이며, 기존 코드의 `moon` 명칭은 제작 이력이다.

사이트 브랜드는 INTO:DAWN이며 헤더·푸터·공유 이미지·메타데이터에 같은 이름을 사용한다. After Hours Til Dawn은 공연 투어 타이틀로 유지한다.

기존 코드에서 추출한 시스템이다. 시각적 권위는 `PRODUCT.md`의 확정된 콘셉트와 승인된 v8 영상에 있으며, 홈의 구체적인 정보 배치는 `docs/eclipse-pamphlet-direction.md`를 따른다. 새로운 시각 세계나 공통 카드 라이브러리를 정의하지 않는다.

**현재 승인 범위의 우선순위:** 이 문서의 보드 배경·홈 및 상세 페이지 구름 규칙과 `docs/share-art-direction.md`의 공유 이미지 규칙은 2026-09-17의 최신 사용자 요청을 반영한다. 이 범위에서는 과거 문서의 투명 글래스 지면, 84% 읽기 배경, 표지 구름·안개의 화면 고정, 공유 실사 에셋 재사용 및 공유 포스터·도구 화면의 `예상 · 보장 아님` 보존 지시보다 우선한다. 기존의 일식 화면 고정·구름 덩어리 금지·공유 아트워크 변경 금지 역시 관련 최신 규칙으로 대체한다. 그 밖의 새 에디션 기준과 이전 기록은 유지한다.

**Key Characteristics:**

- 어두운 바탕, 따뜻한 빛, 선명한 한국어 자료.
- 응축된 영문 타이틀과 굵은 한국어 제목.
- 배경은 느리게 흐르고 본문은 안정적으로 읽히는 지면.

## Colors

따뜻한 앰버가 야간의 상호작용과 장 제목을 안내하며 아이보리가 읽기를 담당한다. 프런트매터가 색상 값의 기준이며 구현 원본은 `src/styles/tokens.css`다.

### Primary

- **앰버 빛** (`amber`): 목차 제목, 보조 라벨, 선택 상태, 기본 키보드 포커스, 티켓 저장 버튼.

### Secondary

- **공연 블루** (`blue`): 실용 정보 배지의 테두리, 미디어 버튼, 티켓 폼의 별도 포커스 처리.
- **공연 레드** (`red`): 기존 공연 자료의 보조 팔레트. 홈에 새 강조색 면적을 추가하는 근거로 삼지 않는다.

### Neutral

- **밤** (`night`)과 **일식 블랙** (`eclipse-black`): 일반 페이지와 홈 일식 배경.
- **아이보리** (`ivory`): 본문과 제목. **안개** (`mist`): 보조 정보와 상태.
- **흐린 잉크** (`ink-muted`): 아이보리를 투명하게 혼합한 설명·내비게이션 텍스트.
- **오류 핑크** (`error`): 공유 도구 오류 문구. 장식 강조에 사용하지 않는다.

## Typography

영문 디스플레이는 Bebas Neue, 한국어 제목과 본문은 Noto Sans KR Variable이다. 헤더는 같은 서체의 사전 로드된 Header 별칭을 사용한다. 제목은 굵고 조밀하며 본문은 줄 높이를 넉넉히 둔다. 눈썹 라벨은 대문자이며 한글 본문은 기본적으로 단어 중간에서 줄바꿈하지 않는다.

일반 h1은 `calc(var(--type-scale) * clamp(2.6rem, 7.5vw, 5.4rem))`이다. h2·h3·본문·눈썹 라벨은 프런트매터의 headline·title·body·label 역할을 따른다. 기본 `--type-scale`은 1, 홈은 0.9이며 간격까지 축소하지 않는다.

**The Cover Type Rule.** 홈의 투어 타이틀은 표지 예외다. 데스크톱은 `--hero-h1: clamp(3.8rem, 8.4vw, 10rem)`과 왼쪽 타이틀·오른쪽 일식을 유지한다. 42rem 이하에서는 `clamp(2.25rem, 12.5vw, 5.25rem)`으로 제목과 지름 46.3vw의 일식을 중앙 정렬한다. 타이틀 두 줄은 같은 글자 크기에 줄 높이 0.95, 두 번째 줄 위 여백 0.12em을 사용한다. 이 수치는 원본 기록이며 새 에디션의 기존 표지 배치는 아래 우선 적용 기준을 따른다.

메인 AFTER HOURS의 색상은 순백색 `#ffffff`다. 기존 등장 시간·크기·위치를 유지한다.

2026-09-17 크기 조정: 메인 AFTER HOURS와 TIL DAWN은 모두 직전 글자 크기의 80%로 표시한다. 일식·구름의 배치에 쓰는 `--hero-h1`은 유지하고 별도 `--hero-title-scale: 0.8`을 적용한다. Bebas Neue 두 줄의 실제 글자 사이 간격은 이전과 같도록 두 번째 줄 여백을 보정한다. 발광 반경·윤곽선 두께·반사광 크기는 역배율 1.25로 유지하며 색상·강도·등장 시간은 바꾸지 않는다. 확인값은 데스크톱 139.968→111.974px(간격 약 37.78px), 모바일 60.45→48.36px(간격 약 16.30px)다.

메인 TIL DAWN은 속이 투명한 아웃라인이다. 백금빛 `#fff8ec` 윤곽선 위에 더 얇은 순백색 `#ffffff` 선을 겹쳐 선명한 발광 필라멘트를 만든다. 채워진 면과 면 내부 SVG 코어 필터는 사용하지 않는다. 주변 발광은 안쪽 `#ffcc98`, 중간 `#f5a65d`, 바깥 `#d17839`로 이어져 일식처럼 연한 금빛에서 오렌지빛으로 퍼진다. 등장 완료 후 윤곽선과 주변 발광은 최대 밝기를 일정하게 유지한다. 발광 색은 기존 색에서 일식의 오렌지 `#ff944c` 쪽으로 20% 혼합한 상태를 유지한다. 주변 발광과 안개 반사광의 강도 +10%, 범위 +15%도 유지한다. 안개 가림 마스크는 밝은 윤곽선만 보호하며 비어 있는 글자 내부에는 안개가 그대로 지나간다. 주변 구름에 반사되는 조명 마스크는 유지한다. TIL DAWN은 4초 지연 후 4초 동안 단일 곡선으로 나타나며 이후 밝기 호흡 없이 최대 밝기를 유지한다. AFTER HOURS는 기존 2초 지연·4초 길이와 중간 키프레임을 유지한다. 구현 기준은 `src/styles/eclipse-impact.css`와 `src/scripts/dawn-sky-renderer.ts`이며 모바일 구도와 모션 감소의 정적 대체를 보존한다. 공유 이미지의 TIL DAWN은 별도 디자인 규칙을 유지한다.

## Layout

공통 지면은 `min(100% - (var(--gutter) * 2), 74rem)` 너비로 가운데 정렬한다. 페이지 바닥 너비는 320px이다. 섹션 간격은 기본과 일식 홈의 별도 토큰을 구분한다.

홈 목차는 세 열이며 42rem 이하에서 한 열로 바뀐다. 홈의 소개는 1:1, 셋리스트와 팬의 한마디는 0.8:1.2 지면이며 48rem 이하에서 한 열이 된다. 고양 바로가기도 0.8:1.2이며 42rem에서 접힌다. 이 홈 확장은 상세 페이지의 기존 구성을 대체하지 않는다.

헤더는 44rem 이하에서 메뉴 disclosure로 바뀌고 링크는 한 열을 채운다. 공유 도구의 폼·미리보기는 48rem 이하에서 쌓인다. 제목과 긴 텍스트는 좁은 화면 및 텍스트 확대에서 줄바꿈한다. 공통 인터랙션 최소 높이는 44px, 티켓의 버튼·select는 48px이다.

## Elevation & Depth

배경 전체에 공통 무채색 필름 그레인을 적용한다. `DawnSky.astro`의 고정 오버레이가 일식 영상·구름·안개와 하단 독립 구름장을 함께 덮고, 읽기 보드와 메뉴 아래에 놓인다. 192px 타일의 미세 노이즈를 `soft-light`로 합성해 검은색과 빛의 색조를 보존한다. 홈에서는 불투명도 0.207을 추가로 5% 높인 0.21735를 사용하고, 다른 페이지는 0.18을 유지한다. AFTER HOURS에는 같은 주파수·시드의 무채색 질감을 글자 내부에 별도로 추가한다. AFTER HOURS의 기존 10% 그레인 강도를 15% 높여 흰색 잉크에 최대 11.5% 명도 변조를 곱하고 글자의 알파·윤곽·크기·간격은 보존한다. 질감 필터는 내부 span, 등장 블러는 외부 span에 적용해 2초 지연·4초 등장 시간을 그대로 유지한다. 정적 질감이므로 추가 애니메이션 프레임을 만들지 않으며, 모션 감소·JavaScript 비활성 상태에서도 같은 질감을 제공한다. 클릭·스크롤·포커스를 가로채지 않는다.

구름 등장 기준(2026-09-17): 표지 구름과 전경 안개의 등장 불투명도는 페이지를 연 시점의 시계로 분리한다. Motion의 선형 범위 매핑으로 1초까지 0, 1~~4초 동안 일정하게 0→1, 4초 이후 1을 유지한다. 탭이 숨겨지면 등장 시계도 일시 정지한다. 일식 영상의 측정 광량 `dawn`은 기존처럼 구름의 색상과 공간별 조명에 적용하지만 등장 속도에는 곱하지 않는다. 독립된 읽기 구름과 모션 감소·WebGL 실패 시 정적 대체는 즉시 이용할 수 있다. 원본 사진의 어두운 구름 몸체가 투명하게 소실되지 않도록 밀도는 정규화한 `luminance^0.72`로 복원한다. 가림 마스크·15% 여백·원의 하단 10~~30% 투명도 감쇠는 이후에 적용하며, 빛의 팔레트와 좌측 10%→6시 100% 조명장은 유지한다. 낮은 표면의 산란광을 살짝 줄여 둥근 능선과 그늘진 몸체를 구분한다. 표지 하단 구름은 전경·중경을 합성한 뒤 한 번만 밀도를 10% 낮춘다. 표지 높이 55~75% 구간에서 감쇠가 부드럽게 적용되어 상단·하단 사이에 경계가 생기지 않는다. 구름의 크기·분포·바람·변형과 별도 안개·읽기 구름은 보존하며, 모션 감소 정적 대체에도 같은 하단 90% 마스크를 적용한다.

자료 지면은 주로 평평한 텍스트와 얇은 반투명 선으로 구분한다. 낮은 불투명도의 투명 글래스 안은 사용자가 거절했으며, 현재 보드는 `../backups/boards-transparency-15-2026-09-17`의 배경 수치로 복원했다. 홈 본문과 `/discover/`·`/setlist/`·`/goyang/`의 읽기 지면은 62.9%, 상단 바는 66.3%, 모바일 펼침 메뉴는 69.7%, 맨 위로 버튼은 73.1%, 일반 푸터는 85% 불투명도다. 홈과 세 구름 상세 페이지의 푸터는 읽기 지면과 같은 62.9%를 사용한다. 읽기 지면·상단 바·모바일 메뉴·구름 페이지 푸터는 10px 흐림과 1.06 채도, 최대 2.5%의 따뜻한 반사광을 공유한다. 투명도 감소 설정에서는 이 지면을 불투명하게 제공한다. 복원 기준 백업은 수정하지 않는다. 홈의 깊이는 기존 v8 일식 영상과 분리된 `golden-cloud-bank-v3.webp` 구름·안개가 만든다. 표지에는 투어 타이틀만 표시하며 부제·일정·카운트다운은 두지 않는다.

홈 표지의 일식·투어 타이틀·구름·안개는 하나의 장면으로 문서 스크롤에 따라 함께 위로 이동한다. 구름과 조명·가림은 표지 내부 좌표를 공유하며 확대·압축하지 않는다. 하단 읽기 지면 뒤에는 같은 텍스처·등속 바람·변형·빛 팔레트를 공유하는 별도 viewport 구름장을 둔다. 이 구름장은 고정된 시드와 가장자리를 부드럽게 겹친 행으로 구성하며 스크롤을 따라 이동하지 않는다. 표지가 끝난 뒤 표지 높이의 1.0~1.15 구간에서 두 장면의 밀도만 혼합하고 텍스처 좌표는 섞지 않는다. 하단 구름은 화면 밖의 일식 테두리와 같은 광원으로 은은한 조명과 이동을 유지한다. `/discover/`·`/setlist/`·`/goyang/`는 이 독립된 읽기 구름장과 보드를 사용하며 일식 영상이나 투어 타이틀을 추가하지 않는다. 모션 감소에서는 정적 대체를 제공하고 비활성 탭에서 배경 모션을 멈춘다. 기본 빠른 전이는 180ms, 장면은 620ms, 느린 전이는 900ms다.

데스크톱(42.001rem 이상)은 원의 하단 20%, 즉 중심에서 아래로 `0.6 × 반지름`인 경계부터 부피 있는 구름층을 추가한다. 기존 구름 텍스처와 바람을 유지하고, 빛을 향한 능선은 따뜻하게 밝히며 접힌 내부에는 음영을 둔다. 이 경계 위로 조밀한 구름층을 확장하지 않는다. 하단 구름은 기존 텍스처·명암을 유지하면서 불규칙한 틈과 낮은 능선으로 부피를 부분적으로 덜어낸다. 추가 구름의 밀도와 빛 차폐량은 이전의 80%로 낮춘다. 정적 대체 구름의 불투명도도 같은 비율로 줄인다. 이 밀도 변화는 기존 구름 좌표와 바람을 따라 움직이며, 일정한 높이의 연속 띠로 만들지 않는다. 구름과 안개는 불규칙한 부드러운 마스크로 작은 무리에 나누고 네 개의 서로 다른 방향·위상으로 흐르게 한다. 각 흐름은 같은 반경 0.065와 각속도 0.112를 사용해 초당 표지 높이의 0.00728만큼(이전 대비 20% 감소) 동일한 속도로 이동한다. 텍스처 배율과 반지름을 보정하므로 앞·뒤·추가 구름의 크기가 달라도 실제 이동 속도는 같다. 형태 변화는 면적을 보존하는 큰·중간·작은 구름 무리로 나눈다. 큰 구역마다 1×1·2×2·3×3으로 서로 다르게 세분하며, 무리마다 응집도·변형 강도·주기를 고정된 난수로 다르게 부여한다. 응집된 무리는 약한 변형으로 덩어리를 유지하고, 풀어지는 무리는 타원형 좌표의 국소 회전으로 가장자리를 길게 풀었다 접는다. 두 전단 변형은 변위와 시간 주기를 유지하면서 공간 파장을 넓혀, 작은 실 같은 꼬리보다 둥근 주름이 변화하게 한다. 느슨한 그룹의 회전·타원 왜곡은 줄여 덩어리의 응집력을 높인다. 무리별 변형은 경계 안에서 끝나고 면적을 보존하며, 구름의 밀도·불투명도·대비를 주기적으로 증감하지 않는다. 바람 그룹도 큰 덩어리와 작은 틈의 두 크기 마스크를 혼합해 나누되, 두 그룹의 가중치 합은 항상 1로 유지한다. 조명과 차폐도 같은 변형 좌표를 사용하므로 접히는 능선의 빛과 그림자가 자연스럽게 갱신된다. 표지 하단은 좌우 전체에 옅은 구름 틈을 약 15% 분산한다. 행마다 위치를 엇갈리게 한 부드러운 타원형 틈에 서로 다른 폭·높이를 부여하며, 상단에서는 변화 없이 하단으로 자연스럽게 적용한다. 가장 옅은 중심에도 해당 구름 밀도의 40%를 남겨 검은 구멍이 되지 않게 한다. 틈은 구름과 동일한 변형·이동 좌표를 따르고 빛 차폐 샘플에도 동일하게 적용한다. 이 국소적인 밀도 조정 외에는 그룹 크기·변형 주기·바람·조명 조건을 유지한다. 일식 조명은 구름 텍스처와 독립된 공간 좌표에서 계산한다. 5시에서 6시 방향의 기존 최대 밝기는 유지한다. 7시를 넘으면 직접광은 약해지지만 간접광은 남긴다. 같은 높이의 6시 조명을 100% 기준으로 화면 왼쪽 끝 10%에서 6시까지 부드럽게 밝아지며, 구름의 현재 위치에 따라 조명을 다시 계산한다. 어두운 구름의 중성 주변광과 TIL DAWN의 국소 반사광도 유지한다. 데스크톱의 7시에서 6시 방향 표지 하단에는 같은 텍스처·변형·등속 바람과 능선 조명을 사용하는 불규칙한 구름 부피를 보강한다. 추가 부피는 원 아래에서 시작하며 기존 하단 20% 가림 한도를 확장하지 않는다. 추가 구름의 배치·빛·가림은 기존 구름과 같은 표지 내부 좌표를 사용하며, 일식·타이틀과 함께 스크롤되어 올라간다. 모바일에는 이 데스크톱 전용 구름층을 추가하지 않고 기존 구도를 유지한다. 구현은 `src/scripts/dawn-sky-renderer.ts`와 `src/components/visual/DawnSky.astro`를 따른다.

구름·안개의 일식 반사광은 승인된 v8 반복 영상에서 분리 추출한 세 색을 공유한다. 외곽 오렌지 발광의 정규화 RGB는 `(1.0, 0.582, 0.299)`, 안쪽 연한 금빛은 `(1.0, 0.760, 0.514)`, 가장 밝은 띠의 아이보리는 `(1.0, 0.968, 0.935)`다. 기존의 노란 금빛 조명 상수를 이 팔레트로 교체해 일반 구름·하단 덩어리·빛기둥·안개를 함께 맞춘다. 기존 공간별 조명 세기와 그늘색·이동·변형은 유지한다. 모션 감소/WebGL 대체 화면은 기존 이미지의 색상을 -8도 회전해 같은 오렌지 방향으로 맞춘다.

원의 하단을 침범하는 조밀한 구름만 기존 불투명도의 10~30%를 불규칙하게 덜어낸다. 두 근경 바람과 같은 좌표·형태 변형을 따르는 부드러운 무늬로 감소량을 나누며, 여러 구름 레이어를 합친 뒤 한 번만 적용한다. 원 밖 구름과 앞쪽 안개의 불투명도는 바꾸지 않는다. 효과 경계는 원 안에서 부드럽게 사라지고, 읽기 지면 뒤 별도 구름장에는 적용하지 않는다. 얇아진 부분의 빛 투과도는 함께 보정한다.

표지 하단의 이전 전경 구름은 제거하고, 가로·세로를 20% 키운 더 응집된 구름으로 교체한다. 같은 텍스처의 (0.23, 0.63)을 중심으로 더 좁은 구름 능선 영역을 확대하며 배율은 `(0.1888889, 0.2833333) / 일식 반지름`이다. 중심은 일식 중심에서 왼쪽으로 반지름의 0.92배(모바일 0.56배), 표지 높이의 86%다. 기존 windA와 같은 초당 표지 높이 0.00728의 바람과 면적 보존 변형을 사용하며 부피·불투명도를 주기적으로 증감하지 않는다. 전경과 기존 구름은 공통 `litCloudColor` 함수를 사용해 빛을 받은 면의 금빛, 약하게 받은 면의 중간색과 그늘색을 일치시킨다. 기존 구름층·일식 상부의 가림 한도·하단 보드 구름장은 유지하며 이 구름도 표지와 함께 스크롤된다.

## Shapes

기본 읽기 요소와 티켓 폼은 각진 모서리다. 셋리스트 필터만 작은 곡률을 사용하고, 원형은 일식과 맨 위로 버튼의 기존 형태다. 목차와 disclosure는 얇은 수평선으로 구분한다. 기존 상세 페이지의 대각선 접힘 장식은 유지하되 일식 홈에서는 비활성화한다.

## Components

- **티켓 버튼:** 앰버 바탕, 밤색 글자, 굵기 900, 아이보리 1px 테두리. 복사 보조 버튼은 투명 바탕과 앰버 60% 테두리다. disabled는 불투명도 0.5와 금지 커서. 별도 hover 색은 구현되어 있지 않다.
- **입력:** 티켓 select는 밤색 바탕, 아이보리 글자·테두리, 라벨은 앰버다. 날짜는 기본 radio를 쓴다. 티켓 폼은 앰버 3px 포커스와 3px 간격을 사용한다. 셋리스트 검색·select·뷰 버튼은 안개색 45% 테두리와 작은 곡률, 앰버 3px 포커스와 2px 간격을 사용한다. 선택된 뷰는 앰버 테두리·글자다.
- **헤더 워드마크:** INTO:DAWN의 글자 사이 간격을 분배해 아래 `THE WEEKND · GOYANG 26`의 자연스러운 너비에 맞춘다. 글자 형태를 가로로 늘리지 않고 기존 글자 크기와 헤더 높이를 유지한다. 이름은 `src/components/chrome/navigation.ts`를 공통 기준으로 사용한다. 웹 전체의 헤더·푸터는 `Wordmark.astro`를 공유하며 쌍점은 글자 높이(1cap)의 상단과 하단에 맞춘 지름 0.11em의 두 원이다. 공유 이미지의 상하단 로고도 같은 지름과 글자 높이를 Canvas 글꼴 측정값으로 계산한다. 쌍점 문자는 접근성 이름과 메타데이터에 유지한다.
- **헤더 내비게이션:** 흐린 아이보리에서 hover·현재 페이지의 아이보리로 바뀌고 앰버 2px 밑줄이 펼쳐진다. 키보드 포커스에도 밑줄이 나타난다. 모바일 메뉴는 기본 details 의미 구조와 읽을 수 있는 링크를 보존한다.
- **팜플렛 목차:** 앰버 그룹 제목 아래 아이보리 텍스트 링크와 오른쪽 화살표를 배치한다. hover·focus에서 앰버와 밑줄을 적용한다. 바깥 선은 앰버 32%, 모바일 그룹 구분선은 아이보리 16%다. 새 에디션의 첫 목차 그룹 제목은 왼쪽으로 2px 광학 보정한다.
- **본문 링크:** 쉬는 상태에도 앰버 70% 밑줄이 있고 hover·focus에서 글자색 밑줄이 펼쳐진다. 해당 외부 링크에는 장식 화살표를 붙인다. 기본 포커스는 앰버 3px, 간격 4px이다.
- **Disclosure:** 위아래 아이보리 28% 선, 굵기 800 summary, 앰버 chevron. 열릴 때 chevron이 회전한다. `scene`·`fast`·`none` 속도로 용도에 맞게 동작하고 JavaScript 없이도 내용을 펼칠 수 있다.
- **상태 배지:** 사각 테두리와 굵기 800 라벨. 기본은 안개색, 예상은 앰버, 실용 정보는 파란 테두리다. 일반 웹 본문의 사실·예상 구분은 유지한다. 공유 포스터와 공유 도구 화면의 `예상 · 보장 아님` 문구는 사용자의 명시적 요청으로 제거했으며, 이 범위에 과거 보존 지시를 다시 적용하지 않는다.
- **공유 대체 컨테이너:** 아이보리 18% 선, 1rem 안쪽 여백, 텍스트·링크·복사 동작. 보편적 카드로 확대하지 않는다.
- **공유 이미지:** `/share/setlist/`와 `/share/ticket/`의 1080×1350 JPEG는 `src/lib/share/cardDesign.ts`의 공통 표지·팔레트·푸터를 사용한다. 홈의 실사 일식·구름 에셋은 쓰지 않으며, Canvas2D로 그린 평면 일식 마크와 검정·아이보리·앰버 활자를 공유한다. THE WEEKND는 60px로 아티스트 역할을 분명히 하고, AFTER HOURS와 속이 빈 TIL DAWN을 모두 124px로 맞춘다. 기존 줄 간격 약 10.36px를 유지하고 기준선을 310px·409.4px로 배치해 제목 블록의 위아래 여백을 맞춘다. 평면 일식의 밝은 면과 광점은 수평 오른쪽 3시 방향을 향한다. 포스터는 예상 38곡의 2단 목록과 기존 업데이트 날짜, 티켓은 D-day·관람일·선택한 세 곡을 담는다. 구체적인 구성과 보존 동작은 `docs/share-art-direction.md`를 따른다.

추출 시 확인한 기존 차이: 셋리스트 필터 배경의 `var(--ink)`는 `src`에 정의가 없다. sidecar는 해당 선언을 그대로 기록하며 임의의 새 색 토큰으로 보정하지 않는다. 이 문서 작성으로 UI를 수정하지 않았다.

## Do's and Don'ts

### Do:

- **Do** 기존 v8 일식 영상과 현재 구름 텍스처·바람을 유지하며, 데스크톱 원 하단 20%에는 승인된 부피 구름과 따뜻한 능선·음영을 적용한다.
- **Do** 자료의 읽기 대비, 키보드 포커스, 모바일 줄바꿈과 모션 감소 대체를 보존한다.
- **Do** 새 화면은 공통 색상·서체·지면을 재사용하고 홈의 표지 예외와 구분한다.
- **Do** 홈 표지의 일식·타이틀·구름·안개를 함께 스크롤하고, 홈 하단과 세 상세 페이지의 읽기 구름장만 화면에 고정한다. 목차부터 본문·푸터까지 하나로 이어지는 반투명 읽기 표면을 유지한다.

### Don't:

- **Don't** 가려진 해를 달로 설명하거나 홈의 승인된 v8 일식을 새로운 그래픽으로 대체한다. 공유 이미지에는 별도로 승인된 평면 일식 마크를 사용한다.
- **Don't** 하단에서 조명 받은 구름을 소실시키거나 화면을 낮처럼 밝힌다.
- **Don't** 구조 변경을 이유로 기존 자료·출처·예상 표시·공유 기능을 삭제하거나 축약한다. 공유 포스터·도구 화면의 `예상 · 보장 아님` 제거는 명시적으로 승인된 예외다.

### 현재 홈 배경과 읽기 표면

2026-09-17 최신 승인: 홈 표지의 일식·투어 타이틀·구름·안개는 문서 흐름과 함께 위로 스크롤된다. 하단 보드의 구름·안개는 별도 viewport 장면으로 유지한다. 읽기 보드와 구름 페이지 푸터는 위 Elevation & Depth의 복원한 배경·흐림 수치를 사용한다. 읽기 패널의 위 경계선은 `rgb(218 177 122 / 35%)`다. 패널 하단의 섹션 간격은 내부 여백으로 확보하고 푸터 위 바깥 여백은 0으로 두어 읽기 표면 사이에 배경이 노출되지 않게 한다. 자료마다 별도 유리 카드를 추가하지 않는다. 이 규칙은 이전의 78%·84% 읽기 배경과 거절된 투명 글래스 지면을 대체한다.

## 별도 새 에디션: 우선 적용

이 파일에는 원본 디자인의 기록이 남아 있다. 새 버전의 일반 기준은 docs/new-edition-design.md를 우선하되, 위에서 명시한 최신 승인 범위와 `docs/share-art-direction.md`가 관련 과거 지시를 대체한다. src/styles/edition.css에 검은 바탕 #0b0b0a, 아이보리 #f1eee7, 앰버 #dab17a, 보조문자 #c0beb7을 정의한다. 82rem 지면, 좌측 고정 목차와 우측 읽기 영역, 48rem 이하 한 열 본문을 사용한다. 표지 타이틀과 일식은 확장하고 콘텐츠와 에셋은 원본을 재사용한다. 상세 페이지도 같은 활자·여백·색상 체계를 적용한다.
