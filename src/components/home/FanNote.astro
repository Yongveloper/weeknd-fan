<section class="fan-note" aria-labelledby="fan-note-title">
  <div class="shell-content fan-note__inner">
    <p class="eyebrow">FROM ONE FAN TO ANOTHER</p>
    <h2 id="fan-note-title">팬의 한마디</h2>
    <p>
      모르는 곡이 있어도 괜찮아요. 가장 크게 남는 순간은 완벽히 아는 노래보다,
      눈앞에서 밤이 새벽으로 바뀌는 장면일 테니까요.
    </p>
    <a class="fan-note__cta" href="/share/ticket/"
      >나만의 D-day 티켓 만들기 <span aria-hidden="true">→</span></a
    >
  </div>
</section>

<style>
  .fan-note {
    padding-block: var(--section-space);
    background: linear-gradient(
      130deg,
      color-mix(in srgb, var(--red) 35%, var(--night)),
      color-mix(in srgb, var(--amber) 20%, var(--night))
    );
  }

  .fan-note__inner {
    display: grid;
    gap: 1.25rem;
    max-width: 48rem;
  }

  p:not(.eyebrow) {
    color: var(--ivory);
    font-size: clamp(1.1rem, 2.5vw, 1.4rem);
    font-weight: 700;
    line-height: 1.7;
  }

  .fan-note__cta {
    color: var(--ivory);
    font-weight: 900;
  }
</style>
