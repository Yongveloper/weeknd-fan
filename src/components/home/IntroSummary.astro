<section
  class="home-entry home-entry--intro fold"
  aria-labelledby="intro-title"
>
  <div class="shell-content home-entry__inner">
    <p class="eyebrow">START HERE / 03 MINUTES</p>
    <h2 id="intro-title">3분 만에 The Weeknd 알기</h2>
    <p>
      밤의 충동과 후회를 영화처럼 노래하던 인물이, 새벽을 향해 자신을 마주하는
      이야기. 이번 공연 전에는 이 세 장의 앨범만 이어 들어도 충분해요.
    </p>
    <a href="/discover/"
      >The Weeknd의 밤으로 들어가기 <span aria-hidden="true">→</span></a
    >
  </div>
</section>

<style>
  .home-entry {
    padding-block: var(--section-space);
  }

  .home-entry__inner {
    display: grid;
    gap: 1.25rem;
    max-width: 48rem;
  }

  p:not(.eyebrow) {
    color: var(--mist);
    font-size: clamp(1rem, 2vw, 1.15rem);
  }

  a {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    min-height: 44px;
    color: var(--ivory);
    font-weight: 800;
  }
</style>
