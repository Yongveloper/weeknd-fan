<lunar-atmosphere aria-hidden="true" data-paused="false" data-progress="0">
  <div class="atmosphere__dense">
    <div class="atmosphere__cloud" data-cloud-motion>
      <img
        class="atmosphere__base"
        src="/visual/atmosphere/diffuse-haze-v2.webp"
        width="1280"
        height="853"
        alt=""
        decoding="async"
      />
      <div class="atmosphere__illumination" data-cloud-illumination>
        <img
          src="/visual/atmosphere/diffuse-haze-v2.webp"
          width="1280"
          height="853"
          alt=""
          decoding="async"
        />
      </div>
    </div>
  </div>
  <div class="atmosphere__mist" data-cloud-motion>
    <img
      class="atmosphere__mist-base"
      src="/visual/atmosphere/diffuse-haze-v2.webp"
      width="1280"
      height="853"
      alt=""
      decoding="async"
    />
    <div class="atmosphere__illumination" data-cloud-illumination>
      <img
        src="/visual/atmosphere/diffuse-haze-v2.webp"
        width="1280"
        height="853"
        alt=""
        decoding="async"
      />
    </div>
  </div>
</lunar-atmosphere>

<script>
  import '../../scripts/lunar-atmosphere';
</script>

<style>
  lunar-atmosphere {
    --moon-light: 1;
    --cloud-opacity: 0.35;
    --dense-opacity: 1;
    --scroll-light: 1;
    --light-origin: 80%;
    position: fixed;
    inset: 0;
    z-index: -1;
    overflow: hidden;
    pointer-events: none;
    mix-blend-mode: screen;
  }
  .atmosphere__dense {
    position: absolute;
    inset: 0;
    opacity: var(--dense-opacity);
  }
  .atmosphere__cloud {
    position: absolute;
    width: max(120vw, 60rem);
    aspect-ratio: 1.5;
    left: -10%;
    bottom: -10vh;
    animation:
      cloud-drift 64s ease-in-out infinite alternate,
      cloud-rise 43s ease-in-out -17s infinite alternate;
    opacity: var(--cloud-opacity);
  }
  img,
  .atmosphere__illumination {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }
  .atmosphere__base {
    opacity: 0.12;
  }
  .atmosphere__illumination {
    opacity: calc(var(--moon-light) * var(--scroll-light));
    mask-image: radial-gradient(
      ellipse 85% 80% at var(--light-origin) 30%,
      black 15%,
      transparent 85%
    );
  }
  .atmosphere__illumination img {
    filter: brightness(1) saturate(0.9);
  }
  lunar-atmosphere[data-paused='true'] [data-cloud-motion] {
    animation-play-state: paused;
  }
  .atmosphere__mist {
    position: absolute;
    width: max(130vw, 70rem);
    aspect-ratio: 1.5;
    left: -15%;
    top: -20vw;
    opacity: 0.16;
    animation:
      cloud-drift 79s ease-in-out -21s infinite alternate-reverse,
      cloud-rise 53s ease-in-out -13s infinite alternate;
  }
  .atmosphere__mist-base {
    opacity: 0.25;
  }
  .atmosphere__mist .atmosphere__illumination {
    mask-image: radial-gradient(
      ellipse 85% 85% at var(--light-origin) 35%,
      black 5%,
      transparent 90%
    );
  }
  @keyframes cloud-drift {
    from {
      translate: -2.5% 0;
    }
    to {
      translate: 2.5% 0;
    }
  }
  @keyframes cloud-rise {
    from {
      transform: translateY(-1.5%) scale(1);
    }
    to {
      transform: translateY(1.5%) scale(1.025);
    }
  }
  @media (max-width: 42rem) {
    lunar-atmosphere {
      --light-origin: 55%;
    }
    .atmosphere__mist {
      width: 250vw;
      left: -75vw;
      top: -5vh;
    }
    .atmosphere__cloud {
      width: 210vw;
      left: -55vw;
      bottom: -5vh;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    [data-cloud-motion] {
      animation: none;
    }
  }
</style>
