/** Header/footer chrome wordmark. Kept in its own module so client code
 *  (src/lib/share/cardDesign.ts) never pulls in navigation.ts's UI dictionaries. */
export const wordmark = {
  name: 'INTO:DAWN',
  edition: 'THE WEEKND · GOYANG 26',
} as const;
