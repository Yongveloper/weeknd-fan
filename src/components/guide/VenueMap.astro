<figure class="venue-map" aria-labelledby="venue-map-caption" data-enter>
  <svg
    id="venue-map"
    viewBox="0 0 720 484"
    role="img"
    aria-labelledby="venue-map-title venue-map-desc"
  >
    <title id="venue-map-title">고양종합운동장 주변 간이 지도</title>
    <desc id="venue-map-desc"
      >대화역·킨텍스역·킨텍스 전시장과 경기장의 위치 관계를 단순화한 개략도.
      인터파크 공연 상세 도면을 바탕으로 다시 그림.</desc
    >

    <g class="roads">
      <rect x="0" y="88" width="720" height="26"></rect>
      <rect x="0" y="268" width="720" height="26"></rect>
      <rect x="0" y="428" width="720" height="26"></rect>
      <rect x="340" y="0" width="26" height="484"></rect>
      <rect x="410" y="0" width="26" height="484"></rect>
      <rect x="520" y="0" width="26" height="484"></rect>
      <rect x="690" y="0" width="26" height="484"></rect>
    </g>

    <g class="block">
      <rect x="552" y="302" width="96" height="58" rx="4"></rect>
      <rect x="552" y="372" width="96" height="48" rx="4"></rect>
      <rect x="10" y="165" width="56" height="60" rx="6"></rect>
    </g>

    <ellipse class="stadium" cx="162" cy="198" rx="88" ry="68"></ellipse>

    <path class="walk" d="M240 119 C236 126 226 132 212 139"></path>

    <g class="pill">
      <rect class="pill--subway" x="150" y="83" width="140" height="36" rx="18"
      ></rect>
      <rect class="pill--gtx" x="520" y="423" width="160" height="36" rx="18"
      ></rect>
    </g>

    <g class="dot">
      <circle cx="600" cy="48" r="7"></circle>
      <circle cx="108" cy="330" r="7"></circle>
    </g>

    <text class="venue-line" x="162" y="175" text-anchor="middle"
      >현대카드 슈퍼콘서트 28</text
    >
    <text class="venue-act" x="162" y="198" text-anchor="middle"
      >The Weeknd</text
    >
    <text class="venue-name" x="162" y="228" text-anchor="middle"
      >고양종합운동장</text
    >
    <text class="place" x="38" y="243" text-anchor="middle">보조경기장</text>
    <text class="on-pill" x="220" y="107" text-anchor="middle">③ 대화역</text>
    <text class="on-pill on-pill--light" x="600" y="447" text-anchor="middle"
      >GTX-A 킨텍스역</text
    >
    <text class="place" x="600" y="326" text-anchor="middle">킨텍스</text>
    <text class="place" x="600" y="346" text-anchor="middle">제1전시장</text>
    <text class="place" x="600" y="392" text-anchor="middle">킨텍스</text>
    <text class="place" x="600" y="411" text-anchor="middle">제2전시장</text>
    <text class="place" x="600" y="74" text-anchor="middle">일산백병원</text>
    <text class="place" x="108" y="356" text-anchor="middle">대화마을</text>
    <text class="walk-label" x="258" y="140">도보 약 3분</text>
  </svg>
  <figcaption id="venue-map-caption">
    인터파크 공연 상세의 오는 길 도면을 바탕으로 다시 그린 개략도. 축척·도로
    수는 실제와 다릅니다.
  </figcaption>
  <p class="venue-map__links">
    <a
      href="https://map.kakao.com/?q=%EA%B3%A0%EC%96%91%EC%A2%85%ED%95%A9%EC%9A%B4%EB%8F%99%EC%9E%A5"
      target="_blank"
      rel="noreferrer">공개 지도에서 위치 확인</a
    ><a href="https://bis.goyang.go.kr/m" target="_blank" rel="noreferrer"
      >고양시 BIS에서 버스 확인</a
    >
  </p>
</figure>

<style>
  .venue-map {
    display: grid;
    gap: 0.8rem;
    max-width: 56rem;
    margin: 0;
    padding: clamp(1rem, 3vw, 2rem);
    border-top: 1px solid color-mix(in srgb, var(--amber) 58%, transparent);
    border-bottom: 1px solid color-mix(in srgb, var(--amber) 36%, transparent);
    background: linear-gradient(
      130deg,
      color-mix(in srgb, var(--amber) 12%, transparent),
      transparent 55%
    );
  }
  svg {
    width: 100%;
    height: auto;
  }
  .roads rect {
    fill: color-mix(in srgb, var(--mist) 30%, var(--night));
  }
  .block rect {
    fill: color-mix(in srgb, var(--ivory) 10%, transparent);
    stroke: color-mix(in srgb, var(--mist) 55%, transparent);
    stroke-width: 2;
  }
  .stadium {
    fill: color-mix(in srgb, var(--red) 45%, var(--night));
    stroke: var(--amber);
    stroke-width: 3;
  }
  .walk {
    fill: none;
    stroke: var(--amber);
    stroke-width: 3;
    stroke-dasharray: 6 6;
    stroke-linecap: round;
  }
  .pill--subway {
    fill: var(--amber);
  }
  .pill--gtx {
    fill: var(--blue);
  }
  .dot circle {
    fill: var(--ivory);
  }
  text {
    fill: var(--ivory);
    font-family: var(--font-body);
    font-weight: 800;
  }
  .venue-line {
    fill: var(--mist);
    font-size: 14px;
  }
  .venue-act {
    font-size: 17px;
  }
  .venue-name {
    font-size: 21px;
    font-weight: 900;
  }
  .place {
    fill: var(--mist);
    font-size: 15px;
  }
  .on-pill {
    fill: var(--night);
    font-size: 18px;
  }
  .on-pill--light {
    fill: var(--ivory);
    font-size: 17px;
  }
  .walk-label {
    fill: var(--amber);
    font-size: 15px;
  }
  figcaption {
    color: var(--mist);
    font-size: 0.9rem;
  }
  .venue-map__links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem 1.25rem;
    margin: 0;
  }
  .venue-map__links a {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    color: var(--ivory);
    font-weight: 800;
  }
</style>
